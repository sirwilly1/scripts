#include <stdio.h>

int main() {
    char *names[] = {"Tim", "Jane", "Matt"};
    int arraySize = sizeof(names) / sizeof(names[0]);

    for (int i = 0; i < arraySize; ++i) {
        printf("Name %d: %s\n", i + 1, names[i]);
    }

    return 0;
}
